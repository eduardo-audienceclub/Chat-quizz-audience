# segredos e config local (NUNCA commitar)
.env
.env.*
.netlify/
.claude/

# (netlify/_private.mjs agora é versionado: versão própria sem segredos —
#  Cal real entra pelas envs CALCOM_BASE_URL / CAL_EMBED_VENDEDORES)

# dependências e arquivos de sistema
node_modules/
.DS_Store
*.log

# material de referência (não faz parte do app)
referencia/
